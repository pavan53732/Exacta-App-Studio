# Exacta-App-Studio
Exacta App Studio is a local-only AI app builder for Windows using deterministic orchestration. AI is untrusted and gated by plans, approvals, and diff validation. All indexing, builds, and file writes run locally. No telemetry. Users control providers and API keys.
